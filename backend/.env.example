# ============================================================
# TML Villa 后端环境变量说明
# ============================================================
# 用法：复制为 .env 并填入实际值
# ============================================================

# Database — Supabase 免费版连接池 (pgbouncer 端口 6543)
# ⚠️ 必须包含 ?pgbouncer=true 参数
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=5"

# Direct URL — 用于 Prisma migrate（直连端口 5432）
DIRECT_DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# JWT Secret (for authentication)
# ⚠️ 生产环境必须设置强密钥，不要使用默认值
JWT_SECRET="your-jwt-secret-here"

# Node Environment
NODE_ENV="development"
PORT=3000

# CORS — 允许的前端来源（逗号分隔）
# 生产环境设置为 Netlify 域名，例如：
# ALLOWED_ORIGINS="https://tml-villa.netlify.app,https://your-custom-domain.com"
# 设置为 * 允许所有来源（不推荐生产使用）
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"

# 缓存 TTL（秒），默认 30 秒
CACHE_TTL_SECONDS=30
