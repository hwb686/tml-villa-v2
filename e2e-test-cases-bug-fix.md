# TML Villa 补充测试用例 - BUG修复验证与深度测试

**文档版本**: v1.0 (架构师深度分析版)  
**创建日期**: 2026-03-06  
**关联BUG**: BUG-016, BUG-017, BUG-018  
**测试范围**: 房源管理、搜索功能、数据一致性  

---

## 📋 测试用例清单

### 【P0】BUG修复验证测试

#### TC-BUG-001: 房源数字字段输入验证
**关联BUG**: BUG-016 - 客人/卧室/床/浴室数量无法修改  
**优先级**: P0 (Critical)  
**测试类型**: 功能测试 + 边界测试

**前置条件**:
- 管理员已登录
- 进入房源管理页面

**测试步骤**:
```
1. 点击"新增民宿"
2. 在"客人数量"字段输入: 0
3. 在"卧室数量"字段输入: 1, 然后清空
4. 在"床铺数量"字段输入: 10
5. 在"浴室数量"字段输入: 0
6. 点击保存
```

**预期结果**:
- ✅ 可以输入0值
- ✅ 可以清空字段（后端使用默认值）
- ✅ 大数值(10+)正常保存
- ✅ 保存后数据正确显示

**边界测试**:
```
测试值: -1, 0, 1, 999, 空字符串, "abc", 特殊字符
预期: 负数拒绝、0允许、正数允许、极大值限制、非法字符提示
```

---

#### TC-BUG-002: 房源编辑时数字字段修改
**关联BUG**: BUG-016  
**优先级**: P0 (Critical)

**测试步骤**:
```
1. 选择一个已有房源点击"编辑"
2. 将客人数量从原值改为0
3. 将卧室数量从原值改为999
4. 点击保存
5. 重新打开编辑对话框验证
```

**预期结果**:
- ✅ 修改后的值正确保存到数据库
- ✅ 重新加载时显示最新值
- ✅ 列表页显示更新后的信息

---

#### TC-BUG-003: 首页搜索 - 按日期范围查询空置房源
**关联BUG**: BUG-017 - 搜索框应按日期查询空置民宿  
**优先级**: P0 (Critical)  
**测试类型**: 功能测试 + 集成测试

**前置条件**:
- 存在多个房源，部分在特定日期已被预订
- 房源库存已初始化

**测试步骤**:
```
1. 打开首页，点击搜索框
2. 选择入住日期: 明天
3. 选择退房日期: 后天
4. 不输入地点，不选择人数
5. 点击搜索
```

**预期结果**:
- ✅ 搜索参数只包含checkIn/checkOut
- ✅ 返回结果只显示有可用库存的房源
- ✅ 已满房的房源不在结果中
- ✅ 日期范围正确传递到后端

**验证库存过滤**:
```
前置: 房源A在3月10日库存为0，房源B有库存
操作: 搜索3月10日-3月11日
预期: 结果包含房源B，不包含房源A
```

---

#### TC-BUG-004: 日期搜索边界条件
**关联BUG**: BUG-017  
**优先级**: P1 (High)

**测试用例**:
| 场景 | 入住日期 | 退房日期 | 预期结果 |
|------|----------|----------|----------|
| 同一天 | 2026-03-10 | 2026-03-10 | 错误提示：至少入住1晚 |
| 过去日期 | 2026-01-01 | 2026-01-02 | 错误提示：不能选择过去日期 |
| 跨月预订 | 2026-03-30 | 2026-04-02 | 正常搜索，正确计算可用性 |
| 超长期 | 2026-03-01 | 2026-06-01 | 限制最大预订天数(如90天) |

---

#### TC-BUG-005: 房源删除功能
**关联BUG**: BUG-018 - 管理后台无法删除房源  
**优先级**: P0 (Critical)

**测试步骤**:
```
1. 管理员登录
2. 进入房源管理页面
3. 选择一个没有关联订单的房源
4. 点击删除按钮
5. 在确认对话框点击"确认"
```

**预期结果**:
- ✅ 确认对话框正确显示房源名称
- ✅ 删除成功提示
- ✅ 房源从列表消失
- ✅ 数据库记录已删除
- ✅ 关联的图片从存储删除

---

#### TC-BUG-006: 删除有关联订单的房源
**关联BUG**: BUG-018  
**优先级**: P1 (High)  
**测试类型**: 异常处理测试

**前置条件**:
- 存在一个房源有关联订单（任何状态）

**测试步骤**:
```
1. 尝试删除该房源
2. 观察系统响应
```

**预期结果** (两种可能):
- 方案A (推荐): 拒绝删除，提示"该房源有关联订单，无法删除"
- 方案B: 允许删除但保留订单记录，房源字段标记为"已删除"

**架构师建议**: 采用方案A，数据完整性优先

---

### 【P1】关联功能深度测试

#### TC-REL-001: 删除房源后收藏列表处理
**优先级**: P1 (High)

**测试步骤**:
```
1. 用户A收藏房源X
2. 管理员删除房源X
3. 用户A查看"我的收藏"
```

**预期结果**:
- ✅ 收藏列表正常加载不报错
- ✅ 房源X显示"已下架"或自动移除
- ✅ 用户可以取消该收藏

---

#### TC-REL-002: 删除房源后订单查看
**优先级**: P1 (High)

**测试步骤**:
```
1. 用户预订房源Y（确认状态）
2. 管理员删除房源Y
3. 用户查看"我的订单"
4. 点击该订单查看详情
```

**预期结果**:
- ✅ 订单列表正常显示
- ✅ 订单详情页显示房源信息（从订单快照读取）
- ✅ 不因为房源删除而报错

---

#### TC-REL-003: 价格修改对未确认订单的影响
**优先级**: P1 (High)

**测试步骤**:
```
1. 用户提交订单（房源价格฿1000）
2. 管理员修改房源价格为฿2000
3. 管理员确认该订单
```

**预期结果**:
- ✅ 订单价格保持原价฿1000（订单创建时的快照）
- ✅ 不因为房源价格变更而修改订单金额

---

### 【P1】并发与数据一致性测试

#### TC-CON-001: 并发预订库存扣减
**优先级**: P1 (High)  
**测试类型**: 并发测试

**测试场景**:
```
房源Z在2026-03-10剩余库存: 1

用户A和用户B同时:
1. 打开房源Z详情页
2. 选择相同日期(2026-03-10 to 2026-03-11)
3. 同时点击"预订"
```

**预期结果**:
- ✅ 只有一个用户预订成功
- ✅ 另一个用户收到"库存不足"提示
- ✅ 数据库库存正确扣减为0
- ✅ 不会产生超卖(overbooking)

**测试方法**:
```bash
# 使用脚本模拟并发请求
node test-concurrent-booking.js --homestay-id=xxx --date=2026-03-10
```

---

#### TC-CON-002: 库存修改时的并发控制
**优先级**: P1 (High)

**测试场景**:
```
管理员正在修改房源A的库存时
用户同时提交该房源的订单
```

**预期结果**:
- ✅ 系统正确处理并发，数据一致
- ✅ 无死锁或数据竞争

---

### 【P2】安全与权限测试

#### TC-SEC-001: 普通用户越权访问
**优先级**: P2 (Medium)

**测试步骤**:
```
1. 以普通用户身份登录，获取token
2. 直接调用API:
   POST /api/homestays (创建房源)
   PUT /api/homestays/:id (修改房源)
   DELETE /api/homestays/:id (删除房源)
```

**预期结果**:
- ✅ 所有请求返回403 Forbidden
- ✅ 错误信息:"需要管理员权限"

---

#### TC-SEC-002: SQL注入测试
**优先级**: P2 (Medium)

**测试参数**:
```
keyword: ' OR 1=1 --
keyword: '; DROP TABLE homestays; --
keyword: <script>alert('xss')</script>
```

**预期结果**:
- ✅ 无SQL注入漏洞
- ✅ 无XSS漏洞，输入被正确转义
- ✅ 返回正常搜索结果或空结果

---

### 【P2】性能与边界测试

#### TC-PERF-001: 大量房源搜索性能
**优先级**: P2 (Medium)

**测试步骤**:
```
1. 数据库中创建1000个房源
2. 进行无筛选条件的搜索
3. 记录响应时间
```

**预期结果**:
- ✅ 响应时间 < 2秒
- ✅ 分页正确，无内存溢出

---

#### TC-BOUND-001: 极端值输入测试
**优先级**: P2 (Medium)

| 字段 | 测试值 | 预期结果 |
|------|--------|----------|
| 价格 | -100 | 拒绝或转为0 |
| 价格 | 999999999 | 限制最大值(如999999) |
| 标题 | 500个字符 | 截断或拒绝 |
| 标题 | `<script>` | 转义存储 |
| 图片URL | 超长URL | 正确处理 |

---

## 📊 测试执行计划

### 阶段1: BUG修复验证 (1天)
- [ ] TC-BUG-001 到 TC-BUG-006
- [ ] 确保三个主要BUG已修复

### 阶段2: 关联功能测试 (1天)
- [ ] TC-REL-001 到 TC-REL-003
- [ ] 验证删除房源的级联影响

### 阶段3: 并发与安全测试 (1天)
- [ ] TC-CON-001, TC-CON-002
- [ ] TC-SEC-001, TC-SEC-002

### 阶段4: 性能与边界测试 (0.5天)
- [ ] TC-PERF-001
- [ ] TC-BOUND-001

---

## 🔧 测试脚本模板

### 并发预订测试脚本
```javascript
// test-concurrent-booking.js
const axios = require('axios');

async function concurrentBookingTest() {
  const promises = [];
  
  // 模拟5个用户同时预订
  for (let i = 0; i < 5; i++) {
    promises.push(
      axios.post('http://localhost:3000/api/bookings', {
        homestayId: 'test-homestay-id',
        checkIn: '2026-03-10',
        checkOut: '2026-03-11',
        guests: 2
      }).catch(err => err.response?.data)
    );
  }
  
  const results = await Promise.all(promises);
  const successCount = results.filter(r => r.data?.code === 200).length;
  
  console.log(`成功预订: ${successCount}/5`);
  console.assert(successCount <= 1, '不应产生超卖！');
}

concurrentBookingTest();
```

---

## 📝 测试报告模板

```markdown
## 测试结果: [用例编号]

**执行日期**: YYYY-MM-DD  
**执行人**: [姓名]  
**环境**: [开发/测试/生产]  

### 测试结论
- [ ] 通过
- [ ] 失败
- [ ] 部分通过

### 详细结果
[详细描述测试过程和观察]

### 截图
[附上关键截图]

### 发现的问题
[如有问题，详细描述]

### 建议
[改进建议]
```

---

## 🎯 架构师建议

### 代码层面改进
1. **数字输入组件**: 封装可复用的NumberInput组件，统一处理边界值
2. **删除确认**: 添加二次确认模态框，显示"该房源有X个关联订单，确认删除？"
3. **软删除**: 考虑实现软删除(deletedAt字段)，保留数据完整性

### 测试流程改进
1. **自动化**: 将P0级用例纳入CI/CD自动化测试
2. **预发布**: 每次发布前必须执行完整的并发测试
3. **监控**: 生产环境监控删除操作和并发预订异常

### 数据完整性策略
```
房源删除策略:
1. 检查关联订单 → 有未完成的订单 → 拒绝删除
2. 检查关联收藏 → 自动从用户收藏列表移除
3. 检查关联库存 → 级联删除库存记录
4. 检查关联评价 → 保留评价但标记房源已删除
5. 软删除实现 → 添加deletedAt字段，前台不显示
```

---

**文档结束**

*本测试文档由架构师基于代码审查生成，确保测试覆盖度和深度*
