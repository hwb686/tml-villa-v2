# ============================================================
# TML Villa 前端环境变量说明
# ============================================================
# 用法：
#   本地开发 → 复制为 .env.local（已在 .gitignore 中，不会提交）
#   生产部署 → 修改 .env.production
# ============================================================

# ⚠️  重要：URL 末尾不加 /api，前端代码（services/api.ts）会自动补充

# 本地开发（后端默认运行在 3000 端口）
# VITE_API_BASE_URL=http://localhost:3000

# 生产环境（Render 部署地址）
VITE_API_BASE_URL=https://tml-villa-api.onrender.com

# -------------------------------------------------------
# Supabase（可选）- 用于直接调用 Edge Functions 加速只读请求
# 配置后，获取房源/分类等只读接口将绕过 Render，直接走 Supabase CDN
# 可显著减少 Render 免费版的冷启动压力
# -------------------------------------------------------
# VITE_SUPABASE_URL=https://xxqkcjmeuvxkjgfrbmoc.supabase.co
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
# VITE_SUPABASE_FUNCTIONS_URL=https://xxqkcjmeuvxkjgfrbmoc.supabase.co/functions/v1
